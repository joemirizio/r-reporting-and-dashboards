library(dplyr)
library(glue)
library(rmarkdown)
library(sendmailR)
library(writexl)

# Get dataset

# Render R Markdown

# Write Excel file

# Create HTML email body

# Set attachments
  
# Send email
# NOTE: You will need a valid SMTP server to send an email
